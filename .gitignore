# Ignore the git and zip files we use
.*
*.zip
.venv/
cfn-lint-env/
venv/

# Ignore program and OS files
.vscode
*.DS_Store
*desktop.ini

# If you need to remove .DS_Store
# find . -name .DS_Store -print0 | xargs -0 git rm -f --ignore-unmatch

# Ignore 
node_modules/
__pycache__/
*.py[cod]

# Project Specific
outputs/*

# Kiro - steering but not specs (unless you do want all)
# Don't ignore .kiro (ignored by .* above), but ignore everything else within, except for steering
!.kiro
# .kiro/*
# !.kiro/steering/
# !.kiro/steering/**/*

# But keep these
!.gitignore
!.github
